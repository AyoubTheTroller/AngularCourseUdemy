export class Ingredient {
    /*public name: string;
    public amount: number;

    constructor(name: string, amount: number){
        this.name = name;
        this.amount = amount;
    }*/
    // OR Using shortcut below
    // SHORTCUT: ASSIGNED DIRECTLY ONLY USING CONSTR

    constructor(public name: string,public amount: number){}
}